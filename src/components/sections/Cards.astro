---
import type { Section } from '@/types/page';
import RichText from '@components/RichText.astro';

interface Props {
    section: Section;
}

const { section } = Astro.props;
---

<section class="cards-section" data-theme={section.theme}>
    <div class="cards-section__inner">
        {(section.title || section.subtitle) && (
            <div class="cards-section__header">
                {section.title && <h2 class="cards-section__title">{section.title}</h2>}
                {section.subtitle && <p class="cards-section__subtitle">{section.subtitle}</p>}
                <RichText doc={section.body} />
            </div>
        )}
        {section.items && section.items.length > 0 && (
            <div class="cards-section__grid">
                {section.items.map(item => (
                    <article class="card">
                        {item.mediaUrl && (
                            <div class="card__media">
                                <img src={item.mediaUrl} alt={item.title ?? ''} class="card__image" />
                            </div>
                        )}
                        <div class="card__body">
                            {item.icon && <span class="card__icon" aria-hidden="true">{item.icon}</span>}
                            {item.title && <h3 class="card__title">{item.title}</h3>}
                            {item.subtitle && <p class="card__subtitle">{item.subtitle}</p>}
                            <RichText doc={item.body} />
                            {item.ctas && item.ctas.length > 0 && (
                                <div class="card__ctas">
                                    {item.ctas.map(cta => (
                                        <a
                                            href={cta.href}
                                            class={`card__cta card__cta--${cta.variant ?? 'ghost'}`}
                                            target={cta.openInNewTab ? '_blank' : undefined}
                                            rel={cta.openInNewTab ? 'noopener noreferrer' : undefined}
                                        >
                                            {cta.label}
                                        </a>
                                    ))}
                                </div>
                            )}
                        </div>
                    </article>
                ))}
            </div>
        )}
    </div>
</section>

<style>
    .cards-section {
        padding: var(--section-padding-y, 4rem) var(--section-padding-x, 1.5rem);
        background-color: var(--section-bg);
        color: var(--section-text);
    }

    .cards-section__inner {
        max-width: var(--container-width, 1200px);
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }

    .cards-section__header {
        text-align: center;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .cards-section__title {
        font-size: clamp(1.5rem, 3vw, 2.25rem);
        font-weight: 700;
        margin: 0;
    }

    .cards-section__subtitle {
        margin: 0 auto;
        max-width: 60ch;
        color: var(--section-text-muted);
    }

    .cards-section__grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem;
    }

    @media (max-width: 900px) {
        .cards-section__grid {
            grid-template-columns: 1fr;
        }
    }

    .card {
        border-top: 2px solid var(--color-accent);
        border-inline: 1px solid var(--section-border);
        border-bottom: 1px solid var(--section-border);
        border-radius: var(--radius-lg, 0.5rem);
        background-color: var(--section-surface);
        overflow: hidden;
        display: flex;
        flex-direction: column;
        transition: box-shadow 0.2s, border-top-color 0.2s;
    }

    .card:hover {
        box-shadow: 0 8px 32px rgba(0, 180, 255, 0.12);
    }

    .card__media {
        aspect-ratio: 16 / 9;
        overflow: hidden;
    }

    .card__image {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .card__body {
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        flex: 1;
    }

    .card__icon {
        font-size: 2rem;
        width: 3.5rem;
        height: 3.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(0, 180, 255, 0.1);
        border-radius: var(--radius-lg, 0.5rem);
        margin-bottom: 0.75rem;
        flex-shrink: 0;
    }

    .card__title {
        font-size: 1.125rem;
        font-weight: 600;
        margin: 0;
    }

    .card__subtitle {
        margin: 0;
        color: var(--section-text-muted);
        font-size: 0.9375rem;
    }

    .card__body :global(ul) {
        list-style: none;
        padding: 0;
        margin: 0.5rem 0 0;
        display: flex;
        flex-direction: column;
        gap: 0.375rem;
    }

    .card__body :global(li) {
        font-size: 0.875rem;
        color: var(--section-text-muted);
        padding-left: 1.25rem;
        position: relative;
        line-height: 1.5;
    }

    .card__body :global(li::before) {
        content: '✓';
        position: absolute;
        left: 0;
        color: var(--color-accent);
        font-weight: 600;
    }

    .card__body :global(h4) {
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--color-accent);
        margin: 1rem 0 0.25rem;
    }

    .card__ctas {
        margin-top: auto;
        padding-top: 1rem;
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .card__cta {
        font-size: 0.875rem;
        font-weight: 600;
        text-decoration: none;
    }

    .card__cta--ghost {
        text-decoration: underline;
    }

    .card__cta--primary {
        padding: 0.5rem 1rem;
        background-color: var(--btn-primary-bg);
        color: var(--btn-primary-text);
        border-radius: 0.25rem;
    }

    .card__cta--secondary {
        padding: 0.5rem 1rem;
        border: 1px solid var(--btn-secondary-border);
        color: var(--btn-secondary-text);
        border-radius: 0.25rem;
    }
</style>
